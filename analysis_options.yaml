include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Non-functional migration/style findings should not block CI while the
    # command-center and workflow-map surfaces evolve independently.
    use_null_aware_elements: ignore
    unused_element_parameter: ignore

linter:
  rules:
    avoid_print: false
    prefer_final_locals: true
